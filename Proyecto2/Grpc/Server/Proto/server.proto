syntax = "proto3";
package band_service;
option go_package = "./;confproto";

message Band {
  string name = 1;
  string album = 2;
  string year = 3;
  string rank = 4;
}

// Definición del servicio BandService que contendrá los métodos para interactuar con el archivo json.
service BandService {
  // Método para enviar información.
  rpc SendBandInfo(Band) returns (BandResponse) {}
}

// Definición del mensaje de respuesta BandResponse.
message BandResponse {
  string message = 1;
}
